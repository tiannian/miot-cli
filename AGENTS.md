# Repository Workflow

After every completed modification, create a Git commit and push it to the configured remote branch. Do not leave completed changes only in the working tree unless the user explicitly asks otherwise.
